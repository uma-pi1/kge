import: [lookup_embedder]

transe:
  class_name: TransE
  entity_embedder:
    type: lookup_embedder
    +++: +++
  relation_embedder:
    type: lookup_embedder
    +++: +++
  l_norm: 1                    # 1, 2

# TODO add entity and relation embedding normalization!
# this is already in the lookup embedders
