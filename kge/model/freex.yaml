import: [lookup_embedder]

freex:
  class_name: Freex
  entity_embedder:
    type: lookup_embedder
    +++: +++
  relation_embedder:
    type: lookup_embedder
    dim: -1  # autoset to 2*entity_embedder.dim
    +++: +++
