job.type: train
dataset.name: toy
model: complex
train:
  optimizer: Adagrad
  optimizer_args:
    lr: 0.2
    weight_decay: 0.4e-7
lookup_embedder.dim: 100
lookup_embedder.initialize: xavier_uniform_

# Evaluation
eval.type: entity_pair_ranking
#eval.type: entity_ranking
eval.max_k: 100
 
